import { BadgeDollarSign } from "lucide-react";

export default function RevenueOutlook({ revenue }) {
  return <section id="money" className="flos-panel flos-revenue"><h3><BadgeDollarSign /> Revenue Outlook</h3>{revenue?.hasData ? <><span>This week</span><b>${Math.round(revenue.value || 0).toLocaleString()}</b>{revenue.potential ? <em>+${Math.round(revenue.potential)}</em> : null}<small>{revenue.potential ? "AI-estimated upside" : "based on available revenue records"}</small><svg viewBox="0 0 210 80" aria-hidden="true"><path d="M2 62 C28 42 34 61 52 45 S82 54 98 30 S126 58 145 28 S176 18 208 8" /><circle cx="170" cy="22" r="4" /></svg></> : <p className="flos-empty">Collecting performance data. Revenue forecast appears after verified earnings, purchases or stat snapshots exist.</p>}<button>View revenue planner →</button></section>;
}