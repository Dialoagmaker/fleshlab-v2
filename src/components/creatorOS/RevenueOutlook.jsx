import { BadgeDollarSign } from "lucide-react";

export default function RevenueOutlook({ revenue, onOpen }) {
  return <section id="money" className="flos-panel flos-revenue"><h3><BadgeDollarSign /> Revenue Outlook</h3><button type="button" className="flos-revenue-open" onClick={onOpen}>{revenue?.hasData ? <><span>This week</span><b>${Math.round(revenue.value || 0).toLocaleString()}</b>{revenue.potential ? <em>+${Math.round(revenue.potential)}</em> : null}<small>{revenue.potential ? "Open forecast logic" : "Open verified revenue details"}</small><svg viewBox="0 0 210 80" aria-hidden="true"><path d="M2 62 C28 42 34 61 52 45 S82 54 98 30 S126 58 145 28 S176 18 208 8" /><circle cx="170" cy="22" r="4" /></svg></> : <p className="flos-empty">No revenue forecast yet. Open Revenue Planner to review verified earnings, purchases and missing signals.</p>}</button><button type="button" onClick={onOpen}>View revenue planner →</button></section>;
}