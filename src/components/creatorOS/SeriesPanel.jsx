import { Clapperboard } from "lucide-react";

export default function SeriesPanel({ series = [], onOpenLibrary, onOpenSeries }) {
  return <section id="library" className="flos-panel flos-series"><h3><Clapperboard /> Series in progress</h3>{series.length ? series.map(row => <button type="button" className="flos-series-row" key={row.name} onClick={() => onOpenSeries?.(row)}><div className="flos-series-mark">{row.episodes}</div><span><b>{row.name}</b><small>{row.missing ? `Episode ${row.missing} ready to produce` : `${row.episodes} episodes detected`}</small><i><em style={{ width: `${row.progress}%` }} /></i></span><strong>{row.progress}%</strong></button>) : <p className="flos-empty">No recurring series detected yet. Open Library Intelligence to review categories and decide what to build next.</p>}<button type="button" onClick={onOpenLibrary}>View library intelligence →</button></section>;
}